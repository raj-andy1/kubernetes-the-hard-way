apiVersion: v1
kind: Pod
metadata:
  labels:
    run: qos-guaranteed
  name: qos-guaranteed
  namespace: cka-practice-test-1
spec:
  containers:
  - image: busybox
    name: qos-guaranteed
    command: ["sh", "-c", "sleep 3600"]
    resources:
      requests:
        memory: 128Mi
        cpu: 200m
      limits:
        memory: 128Mi
        cpu: 200m
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
