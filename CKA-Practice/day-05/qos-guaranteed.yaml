apiVersion: v1
kind: Pod
metadata:
    name: qos-guaranteed
    namespace: cka-practice
spec:
  containers:
  - image: busybox
    name: qos-guaranteed
    command: ["sh", "-c"]
    args: ["sleep 3600"]
    resources: 
      limits:
        memory: "200Mi"
        cpu: "700m"
      requests:
        memory: "200Mi"
        cpu: "700m" 
  dnsPolicy: ClusterFirst
  restartPolicy: Always