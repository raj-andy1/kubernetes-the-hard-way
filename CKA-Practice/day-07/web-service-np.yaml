apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-service-np
  name: web-service-np
  namespace: cka-practice
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    name: web-service-cka
  type: NodePort
status:
  loadBalancer: {}
