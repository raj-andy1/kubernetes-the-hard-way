apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-service
  name: web-service
  namespace: cka-practice
spec:
  ports:
  - name: 80-80
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: web-svc-pod
  type: ClusterIP
status:
  loadBalancer: {}
